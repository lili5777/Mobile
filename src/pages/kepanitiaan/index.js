import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const Kepanitian = () => {
  return (
    <View style={styles.container}>
      <Text>Kepanitian</Text>
    </View>
  );
};

export default Kepanitian;
const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
